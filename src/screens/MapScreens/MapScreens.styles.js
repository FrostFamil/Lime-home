import { StyleSheet } from 'react-native';


const styles = () => {
  
    return StyleSheet.create({
        mainView: {
            flex: 1
        },
        mapView: {
            flex: 1
        }
    });
  };

export default styles;